let x=1;
    let y=2;
    let z= x+y;
    let m = x-y;
    let l=x*y;
    let d=x/y;
    //console.log("the sum result is "+z);
    let resultText="the sum is "+ z+
    " the minus result is "+m+
    " the * result is "+l+
    " the divistion result  "+d;
    document.getElementById("result").innerText=resultText;